JSP JSON Parser
===============

A hand-coded recursive descent json parser in Rust.


TODO:
=====
  - Change parsing API's to return a Result<> instead of an Option<>.
  - Use push-down automata instead of a recursive descent strategy.
